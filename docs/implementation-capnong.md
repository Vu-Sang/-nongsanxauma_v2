# CapNong React implementation

Approved visual source: stitch-reference/code.html, fresh.html, combos.html and the updated olive SVG process illustrations.

1. Create Vite, React, TypeScript and locally compiled Tailwind application.
2. Mechanically convert the approved homepage markup into typed JSX sections; retain imagery, copy, spacing and custom process SVGs. Replace navigation and commerce regions with reusable interactive React components.
3. Add fresh-produce and combo pages, category/search/sort controls, cart quantities and a newsletter demo. Demo AI/account/checkout actions must disclose that no backend service is connected.
4. Verify build, filter/cart logic and rendered desktop/mobile layouts. Open the React preview.

Original preview files remain available as references. Existing staged spec remains untouched. No git identity is invented and no commit is required to run this application.
